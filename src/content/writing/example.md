---
title: "What I learned shipping twelve game jams in a row"
date: 2026-04-12
minutes: 8
tape: NORMAL
side: A
excerpt: "Six lessons that survived; four that didn't."
draft: true
---

This file is a template. `draft: true` keeps it out of the rendered site.
Drop new writing entries alongside it; remove the `draft` line to publish them.

`tape` accepts: `NORMAL`, `CHROME`, `METAL` (drives the cassette type label).
`side` accepts: `A`, `B`.
`minutes` drives both the read-time stamp and the C-XX cassette length spec.
